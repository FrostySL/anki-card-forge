# Only the preview image's pinned asset helper enters the build context.
# Sources, decks, local runtimes and all other project content stay out.
# Runtime scripts still come from the /work mount.
*
!tools/
tools/*
!tools/runtime_assets.py
!tools/runtime-manifest.json
